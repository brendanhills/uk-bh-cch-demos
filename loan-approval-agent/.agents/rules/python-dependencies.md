---
trigger: always_on
glob:
description: Always use uv to run python scripts (e.g., `uv run python ...` or `uv run pytest`).
---
Always use uv to run python scripts (e.g., uv run python ... or uv run pytest).
