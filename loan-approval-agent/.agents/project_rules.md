# Project Rules

This file contains strict rules for this project, captured from user feedback.

## ALWAYS Rules
- Always use Gemini models >= 2.5 (e.g., gemini-2.5-flash, gemini-3-flash-preview).
- Always use uv to run python scripts (e.g., `uv run python ...` or `uv run pytest`).

## NEVER Rules
- (No rules yet)
