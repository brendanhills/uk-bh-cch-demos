---
description: Run the Conductor setup command to initialize the project environment.
---

# Conductor setup

Read `/usr/local/google/home/brendanhills/.gemini/extensions/conductor/commands/conductor/setup.toml` and follow the `prompt` instructions to guide the user through setting up the project.
