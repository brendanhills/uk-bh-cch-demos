---
description: Run the Conductor revert command to undo work.
---

# Conductor revert

Read `/usr/local/google/home/brendanhills/.gemini/extensions/conductor/commands/conductor/revert.toml` and follow the `prompt` instructions to revert work.
