---
description: Run the Conductor implement command to execute tasks.
---

# Conductor implement

Read `commands/implement.toml` and follow the `prompt` instructions to execute the tasks in the plan.
