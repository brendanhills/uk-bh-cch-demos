---
description: Run the Conductor implement command to execute tasks.
---

# Conductor implement

Read `/usr/local/google/home/brendanhills/.gemini/extensions/conductor/commands/conductor/implement.toml` and follow the `prompt` instructions to execute the tasks in the plan.
