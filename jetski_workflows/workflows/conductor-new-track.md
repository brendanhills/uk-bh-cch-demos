---
description: Run the Conductor newTrack command to plan and generate a new track.
---

# Conductor newTrack

Read `/usr/local/google/home/brendanhills/.gemini/extensions/conductor/commands/conductor/newTrack.toml` and follow the `prompt` instructions to guide the user through planning and generating a new track.
