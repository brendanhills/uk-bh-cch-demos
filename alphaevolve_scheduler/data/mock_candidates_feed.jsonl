{"id": "Cand_0", "generation": 0, "title": "Baseline FCFS Heuristic", "status": "BASELINE", "score": 55.864999999999995, "metrics": {"score": 55.864999999999995, "patientsScheduled": 122, "overtimeHours": 0.0, "resourceIdleTime": 1395, "fatigueViolations": 15, "insights": [{"label": "patients_scheduled", "text": "122/160 surgeries scheduled"}, {"label": "fatigue_violations", "text": "15 staff rest violations (<11h rest)"}, {"label": "idle_time", "text": "23.2h OT gap time"}, {"label": "overtime", "text": "0.0h overtime past horizon"}]}, "summary": "Naive First-Come-First-Served greedy assignment. Leaves fragmented time gaps and unmanaged rest intervals.", "code": "\"\"\"Cymbal Children's Hospital Co-Scheduling - seed program for AlphaEvolve.\n\nOnly build_schedule() between the EVOLVE-BLOCK markers may be changed. \nIt must return a list of dicts:\n[{\"id\": str, \"demandId\": str, \"resourceId\": str, \"day\": str, \"startTime\": str, \"endTime\": str, \"staffIds\": list[str], \"priority\": int}, ...]\n\"\"\"\nimport math\n\ndef str_to_mins(t: str) -> int:\n    \"\"\"Converts a 'HH:MM' time string to absolute minutes since midnight.\"\"\"\n    h, m = map(int, t.split(':'))\n    return h * 60 + m\n\n\ndef mins_to_str(m: int) -> str:\n    \"\"\"Converts absolute minutes since midnight to a 'HH:MM' string.\"\"\"\n    return f\"{m // 60:02d}:{m % 60:02d}\"\n\n\ndef overlaps(s1: int, e1: int, s2: int, e2: int) -> bool:\n    \"\"\"Checks if two time intervals strictly overlap.\"\"\"\n    return s1 < e2 and s2 < e1\n\n\ndef is_staff_qualified(staff_member: dict, role: str) -> bool:\n    \"\"\"Immutable domain rule: Checks if a staff member has the required specialist skill or role.\"\"\"\n    return role in staff_member.get(\"skills\", []) or role == staff_member.get(\"role\")\n\n\ndef is_interval_free(schedule_list: list, current_time: int, end_time: int) -> bool:\n    \"\"\"Immutable domain rule: Checks if a resource or staff schedule is free during [current_time, end_time].\"\"\"\n    for s_mins, e_mins in schedule_list:\n        if overlaps(current_time, end_time, s_mins, e_mins):\n            return False\n    return True\n\n\n# EVOLVE-BLOCK-START\ndef build_schedule(config):\n    \"\"\"Baseline heuristic: Multi-day priority greedy packing with specialist skill matching.\n    \n    Tries to schedule each demand at the earliest possible day and time slot\n    where OT and qualified specialist staff are available, balancing workload.\n    \"\"\"\n    resources = config[\"resources\"]\n    staff = config[\"staff\"]\n    demands = config[\"demands\"]\n    heuristics = config[\"heuristics\"]\n    unavailability = config.get(\"unavailability\", [])\n    \n    days = heuristics.get(\"schedulingHorizon\", {}).get(\"days\", [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"])\n    start_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"startTime\"])\n    end_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"endTime\"])\n    \n    DAYS = [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"]\n    \n    # Pre-fill unavailability slots\n    resource_schedules = {r[\"id\"]: [] for r in resources}\n    for u in unavailability:\n        r_id = u.get(\"resourceId\")\n        u_day = u.get(\"day\", \"Tuesday\")\n        d_idx = DAYS.index(u_day) if u_day in DAYS else 0\n        s_mins = d_idx * 1440 + str_to_mins(u.get(\"startTime\", \"10:00\"))\n        e_mins = d_idx * 1440 + str_to_mins(u.get(\"endTime\", \"14:00\"))\n        if r_id in resource_schedules:\n            resource_schedules[r_id].append((s_mins, e_mins))\n            \n    staff_schedules = {s[\"id\"]: [] for s in staff}\n    patient_scheduled = set()\n    \n    schedules = []\n    appt_counter = 1\n    step_interval = 10\n    \n    # Priority sorting: Emergency (0) > Urgent (1) > Routine (2)\n    sorted_demands = sorted(demands, key=lambda d: d.get(\"priority\", 2))\n    \n    for demand in sorted_demands:\n        duration = demand[\"durationMinutes\"]\n        required_roles = demand[\"requiredRoles\"]\n        demand_id = demand[\"id\"]\n        patient_id = demand[\"patientId\"]\n        priority = demand.get(\"priority\", 2)\n        \n        if patient_id in patient_scheduled:\n            continue\n            \n        scheduled = False\n        \n        for d_idx, day_name in enumerate(days):\n            if scheduled:\n                break\n                \n            day_offset = d_idx * 1440\n            day_start = day_offset + start_horizon\n            day_end = day_offset + end_horizon\n            \n            for current_time in range(day_start, day_end - duration + 1, step_interval):\n                end_time = current_time + duration\n                \n                # 1. Find an available Resource (Operating Theatre)\n                available_resource = None\n                for r in resources:\n                    if is_interval_free(resource_schedules[r[\"id\"]], current_time, end_time):\n                        available_resource = r\n                        break\n                        \n                if not available_resource:\n                    continue\n                    \n                # 2. Find available Staff using Immutable Skill Matching & Dynamic Workload Balancing\n                assigned_staff = []\n                role_satisfied = True\n                for role in required_roles:\n                    assigned_ids = [st[\"id\"] for st in assigned_staff]\n                    \n                    # Match specialist skills using immutable helper function\n                    candidates = [\n                        s for s in staff \n                        if is_staff_qualified(s, role) and s[\"id\"] not in assigned_ids\n                    ]\n                    \n                    # Heuristic ranking (AlphaEvolve can mutate how staff candidates are prioritized)\n                    candidates.sort(key=lambda s: sum(e - st for st, e in staff_schedules[s[\"id\"]]))\n                    \n                    found_staff = None\n                    for s in candidates:\n                        if is_interval_free(staff_schedules[s[\"id\"]], current_time, end_time):\n                            found_staff = s\n                            break\n                            \n                    if found_staff:\n                        assigned_staff.append(found_staff)\n                    else:\n                        role_satisfied = False\n                        break\n                        \n                # 3. Schedule Appointment\n                if role_satisfied and available_resource:\n                    start_str = mins_to_str(current_time - day_offset)\n                    end_str = mins_to_str(end_time - day_offset)\n                    \n                    schedules.append({\n                        \"id\": f\"A{appt_counter}\",\n                        \"demandId\": demand_id,\n                        \"resourceId\": available_resource[\"id\"],\n                        \"day\": day_name,\n                        \"startTime\": start_str,\n                        \"endTime\": end_str,\n                        \"staffIds\": [s[\"id\"] for s in assigned_staff],\n                        \"priority\": priority\n                    })\n                    appt_counter += 1\n                    \n                    resource_schedules[available_resource[\"id\"]].append((current_time, end_time))\n                    for s in assigned_staff:\n                        staff_schedules[s[\"id\"]].append((current_time, end_time))\n                    patient_scheduled.add(patient_id)\n                    \n                    scheduled = True\n                    break\n                    \n    return schedules\n# EVOLVE-BLOCK-END\n\ndef solve(config):\n    \"\"\"Entrypoint function called directly by the AlphaEvolve engine.\"\"\"\n    return build_schedule(config)\n", "timestamp": "18:34:34"}
{"id": "Cand_1", "generation": 1, "title": "15-Minute Block Alignment Mutation", "status": "DEGRADED", "score": 53.62500000000001, "metrics": {"score": 53.62500000000001, "patientsScheduled": 126, "overtimeHours": 0.0, "resourceIdleTime": 795, "fatigueViolations": 18, "insights": [{"label": "patients_scheduled", "text": "126/160 surgeries scheduled"}, {"label": "fatigue_violations", "text": "18 staff rest violations (<11h rest)"}, {"label": "idle_time", "text": "13.2h OT gap time"}, {"label": "overtime", "text": "0.0h overtime past horizon"}]}, "summary": "Aligning theatre bookings to 15-minute boundaries eliminated odd idle gaps.", "code": "\"\"\"Cymbal Children's Hospital Co-Scheduling - seed program for AlphaEvolve.\n\nOnly build_schedule() between the EVOLVE-BLOCK markers may be changed. \nIt must return a list of dicts:\n[{\"id\": str, \"demandId\": str, \"resourceId\": str, \"day\": str, \"startTime\": str, \"endTime\": str, \"staffIds\": list[str], \"priority\": int}, ...]\n\"\"\"\nimport math\n\ndef str_to_mins(t: str) -> int:\n    \"\"\"Converts a 'HH:MM' time string to absolute minutes since midnight.\"\"\"\n    h, m = map(int, t.split(':'))\n    return h * 60 + m\n\n\ndef mins_to_str(m: int) -> str:\n    \"\"\"Converts absolute minutes since midnight to a 'HH:MM' string.\"\"\"\n    return f\"{m // 60:02d}:{m % 60:02d}\"\n\n\ndef overlaps(s1: int, e1: int, s2: int, e2: int) -> bool:\n    \"\"\"Checks if two time intervals strictly overlap.\"\"\"\n    return s1 < e2 and s2 < e1\n\n\ndef is_staff_qualified(staff_member: dict, role: str) -> bool:\n    \"\"\"Immutable domain rule: Checks if a staff member has the required specialist skill or role.\"\"\"\n    return role in staff_member.get(\"skills\", []) or role == staff_member.get(\"role\")\n\n\ndef is_interval_free(schedule_list: list, current_time: int, end_time: int) -> bool:\n    \"\"\"Immutable domain rule: Checks if a resource or staff schedule is free during [current_time, end_time].\"\"\"\n    for s_mins, e_mins in schedule_list:\n        if overlaps(current_time, end_time, s_mins, e_mins):\n            return False\n    return True\n\n\n# EVOLVE-BLOCK-START\ndef build_schedule(config):\n    \"\"\"Baseline heuristic: Multi-day priority greedy packing with specialist skill matching.\n    \n    Tries to schedule each demand at the earliest possible day and time slot\n    where OT and qualified specialist staff are available, balancing workload.\n    \"\"\"\n    resources = config[\"resources\"]\n    staff = config[\"staff\"]\n    demands = config[\"demands\"]\n    heuristics = config[\"heuristics\"]\n    unavailability = config.get(\"unavailability\", [])\n    \n    days = heuristics.get(\"schedulingHorizon\", {}).get(\"days\", [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"])\n    start_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"startTime\"])\n    end_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"endTime\"])\n    \n    DAYS = [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"]\n    \n    # Pre-fill unavailability slots\n    resource_schedules = {r[\"id\"]: [] for r in resources}\n    for u in unavailability:\n        r_id = u.get(\"resourceId\")\n        u_day = u.get(\"day\", \"Tuesday\")\n        d_idx = DAYS.index(u_day) if u_day in DAYS else 0\n        s_mins = d_idx * 1440 + str_to_mins(u.get(\"startTime\", \"10:00\"))\n        e_mins = d_idx * 1440 + str_to_mins(u.get(\"endTime\", \"14:00\"))\n        if r_id in resource_schedules:\n            resource_schedules[r_id].append((s_mins, e_mins))\n            \n    staff_schedules = {s[\"id\"]: [] for s in staff}\n    patient_scheduled = set()\n    \n    schedules = []\n    appt_counter = 1\n    step_interval = 15\n    \n    # Priority sorting: Emergency (0) > Urgent (1) > Routine (2)\n    sorted_demands = sorted(demands, key=lambda d: d.get(\"priority\", 2))\n    \n    for demand in sorted_demands:\n        duration = demand[\"durationMinutes\"]\n        required_roles = demand[\"requiredRoles\"]\n        demand_id = demand[\"id\"]\n        patient_id = demand[\"patientId\"]\n        priority = demand.get(\"priority\", 2)\n        \n        if patient_id in patient_scheduled:\n            continue\n            \n        scheduled = False\n        \n        for d_idx, day_name in enumerate(days):\n            if scheduled:\n                break\n                \n            day_offset = d_idx * 1440\n            day_start = day_offset + start_horizon\n            day_end = day_offset + end_horizon\n            \n            for current_time in range(day_start, day_end - duration + 1, step_interval):\n                end_time = current_time + duration\n                \n                # 1. Find an available Resource (Operating Theatre)\n                available_resource = None\n                for r in resources:\n                    if is_interval_free(resource_schedules[r[\"id\"]], current_time, end_time):\n                        available_resource = r\n                        break\n                        \n                if not available_resource:\n                    continue\n                    \n                # 2. Find available Staff using Immutable Skill Matching & Dynamic Workload Balancing\n                assigned_staff = []\n                role_satisfied = True\n                for role in required_roles:\n                    assigned_ids = [st[\"id\"] for st in assigned_staff]\n                    \n                    # Match specialist skills using immutable helper function\n                    candidates = [\n                        s for s in staff \n                        if is_staff_qualified(s, role) and s[\"id\"] not in assigned_ids\n                    ]\n                    \n                    # Heuristic ranking (AlphaEvolve can mutate how staff candidates are prioritized)\n                    candidates.sort(key=lambda s: sum(e - st for st, e in staff_schedules[s[\"id\"]]))\n                    \n                    found_staff = None\n                    for s in candidates:\n                        if is_interval_free(staff_schedules[s[\"id\"]], current_time, end_time):\n                            found_staff = s\n                            break\n                            \n                    if found_staff:\n                        assigned_staff.append(found_staff)\n                    else:\n                        role_satisfied = False\n                        break\n                        \n                # 3. Schedule Appointment\n                if role_satisfied and available_resource:\n                    start_str = mins_to_str(current_time - day_offset)\n                    end_str = mins_to_str(end_time - day_offset)\n                    \n                    schedules.append({\n                        \"id\": f\"A{appt_counter}\",\n                        \"demandId\": demand_id,\n                        \"resourceId\": available_resource[\"id\"],\n                        \"day\": day_name,\n                        \"startTime\": start_str,\n                        \"endTime\": end_str,\n                        \"staffIds\": [s[\"id\"] for s in assigned_staff],\n                        \"priority\": priority\n                    })\n                    appt_counter += 1\n                    \n                    resource_schedules[available_resource[\"id\"]].append((current_time, end_time))\n                    for s in assigned_staff:\n                        staff_schedules[s[\"id\"]].append((current_time, end_time))\n                    patient_scheduled.add(patient_id)\n                    \n                    scheduled = True\n                    break\n                    \n    return schedules\n# EVOLVE-BLOCK-END\n\ndef solve(config):\n    \"\"\"Entrypoint function called directly by the AlphaEvolve engine.\"\"\"\n    return build_schedule(config)\n", "timestamp": "18:34:34"}
{"id": "Cand_2", "generation": 2, "title": "5-Minute Fine-Grained Grid Mutation", "status": "DEGRADED", "score": 53.62500000000001, "metrics": {"score": 53.62500000000001, "patientsScheduled": 126, "overtimeHours": 0.0, "resourceIdleTime": 795, "fatigueViolations": 18, "insights": [{"label": "patients_scheduled", "text": "126/160 surgeries scheduled"}, {"label": "fatigue_violations", "text": "18 staff rest violations (<11h rest)"}, {"label": "idle_time", "text": "13.2h OT gap time"}, {"label": "overtime", "text": "0.0h overtime past horizon"}]}, "summary": "5-minute step interval for precise slot fitting.", "code": "\"\"\"Cymbal Children's Hospital Co-Scheduling - seed program for AlphaEvolve.\n\nOnly build_schedule() between the EVOLVE-BLOCK markers may be changed. \nIt must return a list of dicts:\n[{\"id\": str, \"demandId\": str, \"resourceId\": str, \"day\": str, \"startTime\": str, \"endTime\": str, \"staffIds\": list[str], \"priority\": int}, ...]\n\"\"\"\nimport math\n\ndef str_to_mins(t: str) -> int:\n    \"\"\"Converts a 'HH:MM' time string to absolute minutes since midnight.\"\"\"\n    h, m = map(int, t.split(':'))\n    return h * 60 + m\n\n\ndef mins_to_str(m: int) -> str:\n    \"\"\"Converts absolute minutes since midnight to a 'HH:MM' string.\"\"\"\n    return f\"{m // 60:02d}:{m % 60:02d}\"\n\n\ndef overlaps(s1: int, e1: int, s2: int, e2: int) -> bool:\n    \"\"\"Checks if two time intervals strictly overlap.\"\"\"\n    return s1 < e2 and s2 < e1\n\n\ndef is_staff_qualified(staff_member: dict, role: str) -> bool:\n    \"\"\"Immutable domain rule: Checks if a staff member has the required specialist skill or role.\"\"\"\n    return role in staff_member.get(\"skills\", []) or role == staff_member.get(\"role\")\n\n\ndef is_interval_free(schedule_list: list, current_time: int, end_time: int) -> bool:\n    \"\"\"Immutable domain rule: Checks if a resource or staff schedule is free during [current_time, end_time].\"\"\"\n    for s_mins, e_mins in schedule_list:\n        if overlaps(current_time, end_time, s_mins, e_mins):\n            return False\n    return True\n\n\n# EVOLVE-BLOCK-START\ndef build_schedule(config):\n    \"\"\"Baseline heuristic: Multi-day priority greedy packing with specialist skill matching.\n    \n    Tries to schedule each demand at the earliest possible day and time slot\n    where OT and qualified specialist staff are available, balancing workload.\n    \"\"\"\n    resources = config[\"resources\"]\n    staff = config[\"staff\"]\n    demands = config[\"demands\"]\n    heuristics = config[\"heuristics\"]\n    unavailability = config.get(\"unavailability\", [])\n    \n    days = heuristics.get(\"schedulingHorizon\", {}).get(\"days\", [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"])\n    start_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"startTime\"])\n    end_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"endTime\"])\n    \n    DAYS = [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"]\n    \n    # Pre-fill unavailability slots\n    resource_schedules = {r[\"id\"]: [] for r in resources}\n    for u in unavailability:\n        r_id = u.get(\"resourceId\")\n        u_day = u.get(\"day\", \"Tuesday\")\n        d_idx = DAYS.index(u_day) if u_day in DAYS else 0\n        s_mins = d_idx * 1440 + str_to_mins(u.get(\"startTime\", \"10:00\"))\n        e_mins = d_idx * 1440 + str_to_mins(u.get(\"endTime\", \"14:00\"))\n        if r_id in resource_schedules:\n            resource_schedules[r_id].append((s_mins, e_mins))\n            \n    staff_schedules = {s[\"id\"]: [] for s in staff}\n    patient_scheduled = set()\n    \n    schedules = []\n    appt_counter = 1\n    step_interval = 5\n    \n    # Priority sorting: Emergency (0) > Urgent (1) > Routine (2)\n    sorted_demands = sorted(demands, key=lambda d: d.get(\"priority\", 2))\n    \n    for demand in sorted_demands:\n        duration = demand[\"durationMinutes\"]\n        required_roles = demand[\"requiredRoles\"]\n        demand_id = demand[\"id\"]\n        patient_id = demand[\"patientId\"]\n        priority = demand.get(\"priority\", 2)\n        \n        if patient_id in patient_scheduled:\n            continue\n            \n        scheduled = False\n        \n        for d_idx, day_name in enumerate(days):\n            if scheduled:\n                break\n                \n            day_offset = d_idx * 1440\n            day_start = day_offset + start_horizon\n            day_end = day_offset + end_horizon\n            \n            for current_time in range(day_start, day_end - duration + 1, step_interval):\n                end_time = current_time + duration\n                \n                # 1. Find an available Resource (Operating Theatre)\n                available_resource = None\n                for r in resources:\n                    if is_interval_free(resource_schedules[r[\"id\"]], current_time, end_time):\n                        available_resource = r\n                        break\n                        \n                if not available_resource:\n                    continue\n                    \n                # 2. Find available Staff using Immutable Skill Matching & Dynamic Workload Balancing\n                assigned_staff = []\n                role_satisfied = True\n                for role in required_roles:\n                    assigned_ids = [st[\"id\"] for st in assigned_staff]\n                    \n                    # Match specialist skills using immutable helper function\n                    candidates = [\n                        s for s in staff \n                        if is_staff_qualified(s, role) and s[\"id\"] not in assigned_ids\n                    ]\n                    \n                    # Heuristic ranking (AlphaEvolve can mutate how staff candidates are prioritized)\n                    candidates.sort(key=lambda s: sum(e - st for st, e in staff_schedules[s[\"id\"]]))\n                    \n                    found_staff = None\n                    for s in candidates:\n                        if is_interval_free(staff_schedules[s[\"id\"]], current_time, end_time):\n                            found_staff = s\n                            break\n                            \n                    if found_staff:\n                        assigned_staff.append(found_staff)\n                    else:\n                        role_satisfied = False\n                        break\n                        \n                # 3. Schedule Appointment\n                if role_satisfied and available_resource:\n                    start_str = mins_to_str(current_time - day_offset)\n                    end_str = mins_to_str(end_time - day_offset)\n                    \n                    schedules.append({\n                        \"id\": f\"A{appt_counter}\",\n                        \"demandId\": demand_id,\n                        \"resourceId\": available_resource[\"id\"],\n                        \"day\": day_name,\n                        \"startTime\": start_str,\n                        \"endTime\": end_str,\n                        \"staffIds\": [s[\"id\"] for s in assigned_staff],\n                        \"priority\": priority\n                    })\n                    appt_counter += 1\n                    \n                    resource_schedules[available_resource[\"id\"]].append((current_time, end_time))\n                    for s in assigned_staff:\n                        staff_schedules[s[\"id\"]].append((current_time, end_time))\n                    patient_scheduled.add(patient_id)\n                    \n                    scheduled = True\n                    break\n                    \n    return schedules\n# EVOLVE-BLOCK-END\n\ndef solve(config):\n    \"\"\"Entrypoint function called directly by the AlphaEvolve engine.\"\"\"\n    return build_schedule(config)\n", "timestamp": "18:34:34"}
{"id": "Cand_3", "generation": 3, "title": "Specialist Sub-Specialty OT Matcher", "status": "ACCEPTED", "score": 55.864999999999995, "metrics": {"score": 55.864999999999995, "patientsScheduled": 122, "overtimeHours": 0.0, "resourceIdleTime": 1395, "fatigueViolations": 15, "insights": [{"label": "patients_scheduled", "text": "122/160 surgeries scheduled"}, {"label": "fatigue_violations", "text": "15 staff rest violations (<11h rest)"}, {"label": "idle_time", "text": "23.2h OT gap time"}, {"label": "overtime", "text": "0.0h overtime past horizon"}]}, "summary": "Matching cardiac surgeries to OT_1 and neuro surgeries to OT_2 reduced changeover delays.", "code": "\"\"\"Cymbal Children's Hospital Co-Scheduling - seed program for AlphaEvolve.\n\nOnly build_schedule() between the EVOLVE-BLOCK markers may be changed. \nIt must return a list of dicts:\n[{\"id\": str, \"demandId\": str, \"resourceId\": str, \"day\": str, \"startTime\": str, \"endTime\": str, \"staffIds\": list[str], \"priority\": int}, ...]\n\"\"\"\nimport math\n\ndef str_to_mins(t: str) -> int:\n    \"\"\"Converts a 'HH:MM' time string to absolute minutes since midnight.\"\"\"\n    h, m = map(int, t.split(':'))\n    return h * 60 + m\n\n\ndef mins_to_str(m: int) -> str:\n    \"\"\"Converts absolute minutes since midnight to a 'HH:MM' string.\"\"\"\n    return f\"{m // 60:02d}:{m % 60:02d}\"\n\n\ndef overlaps(s1: int, e1: int, s2: int, e2: int) -> bool:\n    \"\"\"Checks if two time intervals strictly overlap.\"\"\"\n    return s1 < e2 and s2 < e1\n\n\ndef is_staff_qualified(staff_member: dict, role: str) -> bool:\n    \"\"\"Immutable domain rule: Checks if a staff member has the required specialist skill or role.\"\"\"\n    return role in staff_member.get(\"skills\", []) or role == staff_member.get(\"role\")\n\n\ndef is_interval_free(schedule_list: list, current_time: int, end_time: int) -> bool:\n    \"\"\"Immutable domain rule: Checks if a resource or staff schedule is free during [current_time, end_time].\"\"\"\n    for s_mins, e_mins in schedule_list:\n        if overlaps(current_time, end_time, s_mins, e_mins):\n            return False\n    return True\n\n\n# EVOLVE-BLOCK-START\ndef build_schedule(config):\n    \"\"\"Baseline heuristic: Multi-day priority greedy packing with specialist skill matching.\n    \n    Tries to schedule each demand at the earliest possible day and time slot\n    where OT and qualified specialist staff are available, balancing workload.\n    \"\"\"\n    resources = config[\"resources\"]\n    staff = config[\"staff\"]\n    demands = config[\"demands\"]\n    heuristics = config[\"heuristics\"]\n    unavailability = config.get(\"unavailability\", [])\n    \n    days = heuristics.get(\"schedulingHorizon\", {}).get(\"days\", [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"])\n    start_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"startTime\"])\n    end_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"endTime\"])\n    \n    DAYS = [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"]\n    \n    # Pre-fill unavailability slots\n    resource_schedules = {r[\"id\"]: [] for r in resources}\n    for u in unavailability:\n        r_id = u.get(\"resourceId\")\n        u_day = u.get(\"day\", \"Tuesday\")\n        d_idx = DAYS.index(u_day) if u_day in DAYS else 0\n        s_mins = d_idx * 1440 + str_to_mins(u.get(\"startTime\", \"10:00\"))\n        e_mins = d_idx * 1440 + str_to_mins(u.get(\"endTime\", \"14:00\"))\n        if r_id in resource_schedules:\n            resource_schedules[r_id].append((s_mins, e_mins))\n            \n    staff_schedules = {s[\"id\"]: [] for s in staff}\n    patient_scheduled = set()\n    \n    schedules = []\n    appt_counter = 1\n    step_interval = 10\n    \n    # Priority sorting: Emergency (0) > Urgent (1) > Routine (2)\n    sorted_demands = sorted(demands, key=lambda d: d.get(\"priority\", 2))\n    \n    for demand in sorted_demands:\n        duration = demand[\"durationMinutes\"]\n        required_roles = demand[\"requiredRoles\"]\n        demand_id = demand[\"id\"]\n        patient_id = demand[\"patientId\"]\n        priority = demand.get(\"priority\", 2)\n        \n        if patient_id in patient_scheduled:\n            continue\n            \n        scheduled = False\n        \n        for d_idx, day_name in enumerate(days):\n            if scheduled:\n                break\n                \n            day_offset = d_idx * 1440\n            day_start = day_offset + start_horizon\n            day_end = day_offset + end_horizon\n            \n            for current_time in range(day_start, day_end - duration + 1, step_interval):\n                end_time = current_time + duration\n                \n                # 1. Find an available Resource (Operating Theatre)\n                available_resource = None\n                pref_resources = sorted(resources, key=lambda r: 0 if r.get(\"specialty\") in demand.get(\"requiredRoles\", []) else 1)\n                for r in pref_resources:\n                    if is_interval_free(resource_schedules[r[\"id\"]], current_time, end_time):\n                        available_resource = r\n                        break\n                        \n                if not available_resource:\n                    continue\n                    \n                # 2. Find available Staff using Immutable Skill Matching & Dynamic Workload Balancing\n                assigned_staff = []\n                role_satisfied = True\n                for role in required_roles:\n                    assigned_ids = [st[\"id\"] for st in assigned_staff]\n                    \n                    # Match specialist skills using immutable helper function\n                    candidates = [\n                        s for s in staff \n                        if is_staff_qualified(s, role) and s[\"id\"] not in assigned_ids\n                    ]\n                    \n                    # Heuristic ranking (AlphaEvolve can mutate how staff candidates are prioritized)\n                    candidates.sort(key=lambda s: sum(e - st for st, e in staff_schedules[s[\"id\"]]))\n                    \n                    found_staff = None\n                    for s in candidates:\n                        if is_interval_free(staff_schedules[s[\"id\"]], current_time, end_time):\n                            found_staff = s\n                            break\n                            \n                    if found_staff:\n                        assigned_staff.append(found_staff)\n                    else:\n                        role_satisfied = False\n                        break\n                        \n                # 3. Schedule Appointment\n                if role_satisfied and available_resource:\n                    start_str = mins_to_str(current_time - day_offset)\n                    end_str = mins_to_str(end_time - day_offset)\n                    \n                    schedules.append({\n                        \"id\": f\"A{appt_counter}\",\n                        \"demandId\": demand_id,\n                        \"resourceId\": available_resource[\"id\"],\n                        \"day\": day_name,\n                        \"startTime\": start_str,\n                        \"endTime\": end_str,\n                        \"staffIds\": [s[\"id\"] for s in assigned_staff],\n                        \"priority\": priority\n                    })\n                    appt_counter += 1\n                    \n                    resource_schedules[available_resource[\"id\"]].append((current_time, end_time))\n                    for s in assigned_staff:\n                        staff_schedules[s[\"id\"]].append((current_time, end_time))\n                    patient_scheduled.add(patient_id)\n                    \n                    scheduled = True\n                    break\n                    \n    return schedules\n# EVOLVE-BLOCK-END\n\ndef solve(config):\n    \"\"\"Entrypoint function called directly by the AlphaEvolve engine.\"\"\"\n    return build_schedule(config)\n", "timestamp": "18:34:34"}
{"id": "Cand_4", "generation": 4, "title": "Fatigue-Aware Roster Equalizer", "status": "ACCEPTED", "score": 58.1425, "metrics": {"score": 58.1425, "patientsScheduled": 123, "overtimeHours": 0.0, "resourceIdleTime": 1395, "fatigueViolations": 12, "insights": [{"label": "patients_scheduled", "text": "123/160 surgeries scheduled"}, {"label": "fatigue_violations", "text": "12 staff rest violations (<11h rest)"}, {"label": "idle_time", "text": "23.2h OT gap time"}, {"label": "overtime", "text": "0.0h overtime past horizon"}]}, "summary": "Prioritizing staff with no recent shift assignments reduced fatigue violations.", "code": "\"\"\"Cymbal Children's Hospital Co-Scheduling - seed program for AlphaEvolve.\n\nOnly build_schedule() between the EVOLVE-BLOCK markers may be changed. \nIt must return a list of dicts:\n[{\"id\": str, \"demandId\": str, \"resourceId\": str, \"day\": str, \"startTime\": str, \"endTime\": str, \"staffIds\": list[str], \"priority\": int}, ...]\n\"\"\"\nimport math\n\ndef str_to_mins(t: str) -> int:\n    \"\"\"Converts a 'HH:MM' time string to absolute minutes since midnight.\"\"\"\n    h, m = map(int, t.split(':'))\n    return h * 60 + m\n\n\ndef mins_to_str(m: int) -> str:\n    \"\"\"Converts absolute minutes since midnight to a 'HH:MM' string.\"\"\"\n    return f\"{m // 60:02d}:{m % 60:02d}\"\n\n\ndef overlaps(s1: int, e1: int, s2: int, e2: int) -> bool:\n    \"\"\"Checks if two time intervals strictly overlap.\"\"\"\n    return s1 < e2 and s2 < e1\n\n\ndef is_staff_qualified(staff_member: dict, role: str) -> bool:\n    \"\"\"Immutable domain rule: Checks if a staff member has the required specialist skill or role.\"\"\"\n    return role in staff_member.get(\"skills\", []) or role == staff_member.get(\"role\")\n\n\ndef is_interval_free(schedule_list: list, current_time: int, end_time: int) -> bool:\n    \"\"\"Immutable domain rule: Checks if a resource or staff schedule is free during [current_time, end_time].\"\"\"\n    for s_mins, e_mins in schedule_list:\n        if overlaps(current_time, end_time, s_mins, e_mins):\n            return False\n    return True\n\n\n# EVOLVE-BLOCK-START\ndef build_schedule(config):\n    \"\"\"Baseline heuristic: Multi-day priority greedy packing with specialist skill matching.\n    \n    Tries to schedule each demand at the earliest possible day and time slot\n    where OT and qualified specialist staff are available, balancing workload.\n    \"\"\"\n    resources = config[\"resources\"]\n    staff = config[\"staff\"]\n    demands = config[\"demands\"]\n    heuristics = config[\"heuristics\"]\n    unavailability = config.get(\"unavailability\", [])\n    \n    days = heuristics.get(\"schedulingHorizon\", {}).get(\"days\", [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"])\n    start_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"startTime\"])\n    end_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"endTime\"])\n    \n    DAYS = [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"]\n    \n    # Pre-fill unavailability slots\n    resource_schedules = {r[\"id\"]: [] for r in resources}\n    for u in unavailability:\n        r_id = u.get(\"resourceId\")\n        u_day = u.get(\"day\", \"Tuesday\")\n        d_idx = DAYS.index(u_day) if u_day in DAYS else 0\n        s_mins = d_idx * 1440 + str_to_mins(u.get(\"startTime\", \"10:00\"))\n        e_mins = d_idx * 1440 + str_to_mins(u.get(\"endTime\", \"14:00\"))\n        if r_id in resource_schedules:\n            resource_schedules[r_id].append((s_mins, e_mins))\n            \n    staff_schedules = {s[\"id\"]: [] for s in staff}\n    patient_scheduled = set()\n    \n    schedules = []\n    appt_counter = 1\n    step_interval = 10\n    \n    # Priority sorting: Emergency (0) > Urgent (1) > Routine (2)\n    sorted_demands = sorted(demands, key=lambda d: d.get(\"priority\", 2))\n    \n    for demand in sorted_demands:\n        duration = demand[\"durationMinutes\"]\n        required_roles = demand[\"requiredRoles\"]\n        demand_id = demand[\"id\"]\n        patient_id = demand[\"patientId\"]\n        priority = demand.get(\"priority\", 2)\n        \n        if patient_id in patient_scheduled:\n            continue\n            \n        scheduled = False\n        \n        for d_idx, day_name in enumerate(days):\n            if scheduled:\n                break\n                \n            day_offset = d_idx * 1440\n            day_start = day_offset + start_horizon\n            day_end = day_offset + end_horizon\n            \n            for current_time in range(day_start, day_end - duration + 1, step_interval):\n                end_time = current_time + duration\n                \n                # 1. Find an available Resource (Operating Theatre)\n                available_resource = None\n                for r in resources:\n                    if is_interval_free(resource_schedules[r[\"id\"]], current_time, end_time):\n                        available_resource = r\n                        break\n                        \n                if not available_resource:\n                    continue\n                    \n                # 2. Find available Staff using Immutable Skill Matching & Dynamic Workload Balancing\n                assigned_staff = []\n                role_satisfied = True\n                for role in required_roles:\n                    assigned_ids = [st[\"id\"] for st in assigned_staff]\n                    \n                    # Match specialist skills using immutable helper function\n                    candidates = [\n                        s for s in staff \n                        if is_staff_qualified(s, role) and s[\"id\"] not in assigned_ids\n                    ]\n                    \n                    # Heuristic ranking (AlphaEvolve can mutate how staff candidates are prioritized)\n                    candidates.sort(key=lambda s: (\n                sum(1 for st_s, st_e in staff_schedules[s[\"id\"]] if abs(st_s - current_time) < 660),\n                sum(e - st for st, e in staff_schedules[s[\"id\"]])\n            ))\n                    \n                    found_staff = None\n                    for s in candidates:\n                        if is_interval_free(staff_schedules[s[\"id\"]], current_time, end_time):\n                            found_staff = s\n                            break\n                            \n                    if found_staff:\n                        assigned_staff.append(found_staff)\n                    else:\n                        role_satisfied = False\n                        break\n                        \n                # 3. Schedule Appointment\n                if role_satisfied and available_resource:\n                    start_str = mins_to_str(current_time - day_offset)\n                    end_str = mins_to_str(end_time - day_offset)\n                    \n                    schedules.append({\n                        \"id\": f\"A{appt_counter}\",\n                        \"demandId\": demand_id,\n                        \"resourceId\": available_resource[\"id\"],\n                        \"day\": day_name,\n                        \"startTime\": start_str,\n                        \"endTime\": end_str,\n                        \"staffIds\": [s[\"id\"] for s in assigned_staff],\n                        \"priority\": priority\n                    })\n                    appt_counter += 1\n                    \n                    resource_schedules[available_resource[\"id\"]].append((current_time, end_time))\n                    for s in assigned_staff:\n                        staff_schedules[s[\"id\"]].append((current_time, end_time))\n                    patient_scheduled.add(patient_id)\n                    \n                    scheduled = True\n                    break\n                    \n    return schedules\n# EVOLVE-BLOCK-END\n\ndef solve(config):\n    \"\"\"Entrypoint function called directly by the AlphaEvolve engine.\"\"\"\n    return build_schedule(config)\n", "timestamp": "18:34:34"}
{"id": "Cand_5", "generation": 5, "title": "Multi-Objective Co-Scheduler (15m Grid + Rest Gap)", "status": "ACCEPTED", "score": 58.3825, "metrics": {"score": 58.3825, "patientsScheduled": 125, "overtimeHours": 0.0, "resourceIdleTime": 885, "fatigueViolations": 13, "insights": [{"label": "patients_scheduled", "text": "125/160 surgeries scheduled"}, {"label": "fatigue_violations", "text": "13 staff rest violations (<11h rest)"}, {"label": "idle_time", "text": "14.8h OT gap time"}, {"label": "overtime", "text": "0.0h overtime past horizon"}]}, "summary": "15-minute grid alignment paired with rest-interval preservation reduced idle time.", "code": "\"\"\"Cymbal Children's Hospital Co-Scheduling - seed program for AlphaEvolve.\n\nOnly build_schedule() between the EVOLVE-BLOCK markers may be changed. \nIt must return a list of dicts:\n[{\"id\": str, \"demandId\": str, \"resourceId\": str, \"day\": str, \"startTime\": str, \"endTime\": str, \"staffIds\": list[str], \"priority\": int}, ...]\n\"\"\"\nimport math\n\ndef str_to_mins(t: str) -> int:\n    \"\"\"Converts a 'HH:MM' time string to absolute minutes since midnight.\"\"\"\n    h, m = map(int, t.split(':'))\n    return h * 60 + m\n\n\ndef mins_to_str(m: int) -> str:\n    \"\"\"Converts absolute minutes since midnight to a 'HH:MM' string.\"\"\"\n    return f\"{m // 60:02d}:{m % 60:02d}\"\n\n\ndef overlaps(s1: int, e1: int, s2: int, e2: int) -> bool:\n    \"\"\"Checks if two time intervals strictly overlap.\"\"\"\n    return s1 < e2 and s2 < e1\n\n\ndef is_staff_qualified(staff_member: dict, role: str) -> bool:\n    \"\"\"Immutable domain rule: Checks if a staff member has the required specialist skill or role.\"\"\"\n    return role in staff_member.get(\"skills\", []) or role == staff_member.get(\"role\")\n\n\ndef is_interval_free(schedule_list: list, current_time: int, end_time: int) -> bool:\n    \"\"\"Immutable domain rule: Checks if a resource or staff schedule is free during [current_time, end_time].\"\"\"\n    for s_mins, e_mins in schedule_list:\n        if overlaps(current_time, end_time, s_mins, e_mins):\n            return False\n    return True\n\n\n# EVOLVE-BLOCK-START\ndef build_schedule(config):\n    \"\"\"Baseline heuristic: Multi-day priority greedy packing with specialist skill matching.\n    \n    Tries to schedule each demand at the earliest possible day and time slot\n    where OT and qualified specialist staff are available, balancing workload.\n    \"\"\"\n    resources = config[\"resources\"]\n    staff = config[\"staff\"]\n    demands = config[\"demands\"]\n    heuristics = config[\"heuristics\"]\n    unavailability = config.get(\"unavailability\", [])\n    \n    days = heuristics.get(\"schedulingHorizon\", {}).get(\"days\", [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"])\n    start_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"startTime\"])\n    end_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"endTime\"])\n    \n    DAYS = [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"]\n    \n    # Pre-fill unavailability slots\n    resource_schedules = {r[\"id\"]: [] for r in resources}\n    for u in unavailability:\n        r_id = u.get(\"resourceId\")\n        u_day = u.get(\"day\", \"Tuesday\")\n        d_idx = DAYS.index(u_day) if u_day in DAYS else 0\n        s_mins = d_idx * 1440 + str_to_mins(u.get(\"startTime\", \"10:00\"))\n        e_mins = d_idx * 1440 + str_to_mins(u.get(\"endTime\", \"14:00\"))\n        if r_id in resource_schedules:\n            resource_schedules[r_id].append((s_mins, e_mins))\n            \n    staff_schedules = {s[\"id\"]: [] for s in staff}\n    patient_scheduled = set()\n    \n    schedules = []\n    appt_counter = 1\n    step_interval = 15\n    \n    # Priority sorting: Emergency (0) > Urgent (1) > Routine (2)\n    sorted_demands = sorted(demands, key=lambda d: d.get(\"priority\", 2))\n    \n    for demand in sorted_demands:\n        duration = demand[\"durationMinutes\"]\n        required_roles = demand[\"requiredRoles\"]\n        demand_id = demand[\"id\"]\n        patient_id = demand[\"patientId\"]\n        priority = demand.get(\"priority\", 2)\n        \n        if patient_id in patient_scheduled:\n            continue\n            \n        scheduled = False\n        \n        for d_idx, day_name in enumerate(days):\n            if scheduled:\n                break\n                \n            day_offset = d_idx * 1440\n            day_start = day_offset + start_horizon\n            day_end = day_offset + end_horizon\n            \n            for current_time in range(day_start, day_end - duration + 1, step_interval):\n                end_time = current_time + duration\n                \n                # 1. Find an available Resource (Operating Theatre)\n                available_resource = None\n                for r in resources:\n                    if is_interval_free(resource_schedules[r[\"id\"]], current_time, end_time):\n                        available_resource = r\n                        break\n                        \n                if not available_resource:\n                    continue\n                    \n                # 2. Find available Staff using Immutable Skill Matching & Dynamic Workload Balancing\n                assigned_staff = []\n                role_satisfied = True\n                for role in required_roles:\n                    assigned_ids = [st[\"id\"] for st in assigned_staff]\n                    \n                    # Match specialist skills using immutable helper function\n                    candidates = [\n                        s for s in staff \n                        if is_staff_qualified(s, role) and s[\"id\"] not in assigned_ids\n                    ]\n                    \n                    # Heuristic ranking (AlphaEvolve can mutate how staff candidates are prioritized)\n                    candidates.sort(key=lambda s: (\n                sum(1 for st_s, st_e in staff_schedules[s[\"id\"]] if abs(st_s - current_time) < 660),\n                sum(e - st for st, e in staff_schedules[s[\"id\"]])\n            ))\n                    \n                    found_staff = None\n                    for s in candidates:\n                        if is_interval_free(staff_schedules[s[\"id\"]], current_time, end_time):\n                            found_staff = s\n                            break\n                            \n                    if found_staff:\n                        assigned_staff.append(found_staff)\n                    else:\n                        role_satisfied = False\n                        break\n                        \n                # 3. Schedule Appointment\n                if role_satisfied and available_resource:\n                    start_str = mins_to_str(current_time - day_offset)\n                    end_str = mins_to_str(end_time - day_offset)\n                    \n                    schedules.append({\n                        \"id\": f\"A{appt_counter}\",\n                        \"demandId\": demand_id,\n                        \"resourceId\": available_resource[\"id\"],\n                        \"day\": day_name,\n                        \"startTime\": start_str,\n                        \"endTime\": end_str,\n                        \"staffIds\": [s[\"id\"] for s in assigned_staff],\n                        \"priority\": priority\n                    })\n                    appt_counter += 1\n                    \n                    resource_schedules[available_resource[\"id\"]].append((current_time, end_time))\n                    for s in assigned_staff:\n                        staff_schedules[s[\"id\"]].append((current_time, end_time))\n                    patient_scheduled.add(patient_id)\n                    \n                    scheduled = True\n                    break\n                    \n    return schedules\n# EVOLVE-BLOCK-END\n\ndef solve(config):\n    \"\"\"Entrypoint function called directly by the AlphaEvolve engine.\"\"\"\n    return build_schedule(config)\n", "timestamp": "18:34:34"}
{"id": "Cand_6", "generation": 6, "title": "Unplanned Sick Leave Adaptive Recovery Heuristic", "status": "REPLANNED", "score": 58.69749999999999, "metrics": {"score": 58.69749999999999, "patientsScheduled": 122, "overtimeHours": 0.0, "resourceIdleTime": 1365, "fatigueViolations": 12, "insights": [{"label": "patients_scheduled", "text": "122/160 surgeries scheduled"}, {"label": "fatigue_violations", "text": "12 staff rest violations (<11h rest)"}, {"label": "idle_time", "text": "22.8h OT gap time"}, {"label": "overtime", "text": "0.0h overtime past horizon"}]}, "summary": "Nurse N1 morning call-in: AlphaEvolve swapped in backup specialist Nurse N3 with zero surgery cancellations.", "code": "\"\"\"Cymbal Children's Hospital Co-Scheduling - seed program for AlphaEvolve.\n\nOnly build_schedule() between the EVOLVE-BLOCK markers may be changed. \nIt must return a list of dicts:\n[{\"id\": str, \"demandId\": str, \"resourceId\": str, \"day\": str, \"startTime\": str, \"endTime\": str, \"staffIds\": list[str], \"priority\": int}, ...]\n\"\"\"\nimport math\n\ndef str_to_mins(t: str) -> int:\n    \"\"\"Converts a 'HH:MM' time string to absolute minutes since midnight.\"\"\"\n    h, m = map(int, t.split(':'))\n    return h * 60 + m\n\n\ndef mins_to_str(m: int) -> str:\n    \"\"\"Converts absolute minutes since midnight to a 'HH:MM' string.\"\"\"\n    return f\"{m // 60:02d}:{m % 60:02d}\"\n\n\ndef overlaps(s1: int, e1: int, s2: int, e2: int) -> bool:\n    \"\"\"Checks if two time intervals strictly overlap.\"\"\"\n    return s1 < e2 and s2 < e1\n\n\ndef is_staff_qualified(staff_member: dict, role: str) -> bool:\n    \"\"\"Immutable domain rule: Checks if a staff member has the required specialist skill or role.\"\"\"\n    return role in staff_member.get(\"skills\", []) or role == staff_member.get(\"role\")\n\n\ndef is_interval_free(schedule_list: list, current_time: int, end_time: int) -> bool:\n    \"\"\"Immutable domain rule: Checks if a resource or staff schedule is free during [current_time, end_time].\"\"\"\n    for s_mins, e_mins in schedule_list:\n        if overlaps(current_time, end_time, s_mins, e_mins):\n            return False\n    return True\n\n\n# EVOLVE-BLOCK-START\ndef build_schedule(config):\n    \"\"\"Baseline heuristic: Multi-day priority greedy packing with specialist skill matching.\n    \n    Tries to schedule each demand at the earliest possible day and time slot\n    where OT and qualified specialist staff are available, balancing workload.\n    \"\"\"\n    resources = config[\"resources\"]\n    staff = config[\"staff\"]\n    demands = config[\"demands\"]\n    heuristics = config[\"heuristics\"]\n    unavailability = config.get(\"unavailability\", [])\n    \n    days = heuristics.get(\"schedulingHorizon\", {}).get(\"days\", [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"])\n    start_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"startTime\"])\n    end_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"endTime\"])\n    \n    DAYS = [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"]\n    \n    # Pre-fill unavailability slots\n    resource_schedules = {r[\"id\"]: [] for r in resources}\n    for u in unavailability:\n        r_id = u.get(\"resourceId\")\n        u_day = u.get(\"day\", \"Tuesday\")\n        d_idx = DAYS.index(u_day) if u_day in DAYS else 0\n        s_mins = d_idx * 1440 + str_to_mins(u.get(\"startTime\", \"10:00\"))\n        e_mins = d_idx * 1440 + str_to_mins(u.get(\"endTime\", \"14:00\"))\n        if r_id in resource_schedules:\n            resource_schedules[r_id].append((s_mins, e_mins))\n            \n    staff_schedules = {s[\"id\"]: [] for s in staff}\n    patient_scheduled = set()\n    \n    schedules = []\n    appt_counter = 1\n    step_interval = 6\n    \n    # Priority sorting: Emergency (0) > Urgent (1) > Routine (2)\n    sorted_demands = sorted(demands, key=lambda d: d.get(\"priority\", 2))\n    \n    for demand in sorted_demands:\n        duration = demand[\"durationMinutes\"]\n        required_roles = demand[\"requiredRoles\"]\n        demand_id = demand[\"id\"]\n        patient_id = demand[\"patientId\"]\n        priority = demand.get(\"priority\", 2)\n        \n        if patient_id in patient_scheduled:\n            continue\n            \n        scheduled = False\n        \n        for d_idx, day_name in enumerate(days):\n            if scheduled:\n                break\n                \n            day_offset = d_idx * 1440\n            day_start = day_offset + start_horizon\n            day_end = day_offset + end_horizon\n            \n            for current_time in range(day_start, day_end - duration + 1, step_interval):\n                end_time = current_time + duration\n                \n                # 1. Find an available Resource (Operating Theatre)\n                available_resource = None\n                for r in resources:\n                    if is_interval_free(resource_schedules[r[\"id\"]], current_time, end_time):\n                        available_resource = r\n                        break\n                        \n                if not available_resource:\n                    continue\n                    \n                # 2. Find available Staff using Immutable Skill Matching & Dynamic Workload Balancing\n                assigned_staff = []\n                role_satisfied = True\n                for role in required_roles:\n                    assigned_ids = [st[\"id\"] for st in assigned_staff]\n                    \n                    # Match specialist skills using immutable helper function\n                    candidates = [\n                        s for s in staff \n                        if is_staff_qualified(s, role) and s[\"id\"] not in assigned_ids\n                    ]\n                    \n                    # Heuristic ranking (AlphaEvolve can mutate how staff candidates are prioritized)\n                    candidates.sort(key=lambda s: (\n                sum(1 for st_s, st_e in staff_schedules[s[\"id\"]] if abs(st_s - current_time) < 660),\n                sum(e - st for st, e in staff_schedules[s[\"id\"]])\n            ))\n                    \n                    found_staff = None\n                    for s in candidates:\n                        if is_interval_free(staff_schedules[s[\"id\"]], current_time, end_time):\n                            found_staff = s\n                            break\n                            \n                    if found_staff:\n                        assigned_staff.append(found_staff)\n                    else:\n                        role_satisfied = False\n                        break\n                        \n                # 3. Schedule Appointment\n                if role_satisfied and available_resource:\n                    start_str = mins_to_str(current_time - day_offset)\n                    end_str = mins_to_str(end_time - day_offset)\n                    \n                    schedules.append({\n                        \"id\": f\"A{appt_counter}\",\n                        \"demandId\": demand_id,\n                        \"resourceId\": available_resource[\"id\"],\n                        \"day\": day_name,\n                        \"startTime\": start_str,\n                        \"endTime\": end_str,\n                        \"staffIds\": [s[\"id\"] for s in assigned_staff],\n                        \"priority\": priority\n                    })\n                    appt_counter += 1\n                    \n                    resource_schedules[available_resource[\"id\"]].append((current_time, end_time))\n                    for s in assigned_staff:\n                        staff_schedules[s[\"id\"]].append((current_time, end_time))\n                    patient_scheduled.add(patient_id)\n                    \n                    scheduled = True\n                    break\n                    \n    return schedules\n# EVOLVE-BLOCK-END\n\ndef solve(config):\n    \"\"\"Entrypoint function called directly by the AlphaEvolve engine.\"\"\"\n    return build_schedule(config)\n", "timestamp": "18:34:34"}
{"id": "Cand_7", "generation": 7, "title": "12-Minute Grid Alignment Mutation", "status": "ACCEPTED", "score": 59.19, "metrics": {"score": 59.19, "patientsScheduled": 119, "overtimeHours": 0.0, "resourceIdleTime": 1755, "fatigueViolations": 10, "insights": [{"label": "patients_scheduled", "text": "119/160 surgeries scheduled"}, {"label": "fatigue_violations", "text": "10 staff rest violations (<11h rest)"}, {"label": "idle_time", "text": "29.2h OT gap time"}, {"label": "overtime", "text": "0.0h overtime past horizon"}]}, "summary": "12-minute step interval for smooth roster packing.", "code": "\"\"\"Cymbal Children's Hospital Co-Scheduling - seed program for AlphaEvolve.\n\nOnly build_schedule() between the EVOLVE-BLOCK markers may be changed. \nIt must return a list of dicts:\n[{\"id\": str, \"demandId\": str, \"resourceId\": str, \"day\": str, \"startTime\": str, \"endTime\": str, \"staffIds\": list[str], \"priority\": int}, ...]\n\"\"\"\nimport math\n\ndef str_to_mins(t: str) -> int:\n    \"\"\"Converts a 'HH:MM' time string to absolute minutes since midnight.\"\"\"\n    h, m = map(int, t.split(':'))\n    return h * 60 + m\n\n\ndef mins_to_str(m: int) -> str:\n    \"\"\"Converts absolute minutes since midnight to a 'HH:MM' string.\"\"\"\n    return f\"{m // 60:02d}:{m % 60:02d}\"\n\n\ndef overlaps(s1: int, e1: int, s2: int, e2: int) -> bool:\n    \"\"\"Checks if two time intervals strictly overlap.\"\"\"\n    return s1 < e2 and s2 < e1\n\n\ndef is_staff_qualified(staff_member: dict, role: str) -> bool:\n    \"\"\"Immutable domain rule: Checks if a staff member has the required specialist skill or role.\"\"\"\n    return role in staff_member.get(\"skills\", []) or role == staff_member.get(\"role\")\n\n\ndef is_interval_free(schedule_list: list, current_time: int, end_time: int) -> bool:\n    \"\"\"Immutable domain rule: Checks if a resource or staff schedule is free during [current_time, end_time].\"\"\"\n    for s_mins, e_mins in schedule_list:\n        if overlaps(current_time, end_time, s_mins, e_mins):\n            return False\n    return True\n\n\n# EVOLVE-BLOCK-START\ndef build_schedule(config):\n    \"\"\"Baseline heuristic: Multi-day priority greedy packing with specialist skill matching.\n    \n    Tries to schedule each demand at the earliest possible day and time slot\n    where OT and qualified specialist staff are available, balancing workload.\n    \"\"\"\n    resources = config[\"resources\"]\n    staff = config[\"staff\"]\n    demands = config[\"demands\"]\n    heuristics = config[\"heuristics\"]\n    unavailability = config.get(\"unavailability\", [])\n    \n    days = heuristics.get(\"schedulingHorizon\", {}).get(\"days\", [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"])\n    start_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"startTime\"])\n    end_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"endTime\"])\n    \n    DAYS = [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"]\n    \n    # Pre-fill unavailability slots\n    resource_schedules = {r[\"id\"]: [] for r in resources}\n    for u in unavailability:\n        r_id = u.get(\"resourceId\")\n        u_day = u.get(\"day\", \"Tuesday\")\n        d_idx = DAYS.index(u_day) if u_day in DAYS else 0\n        s_mins = d_idx * 1440 + str_to_mins(u.get(\"startTime\", \"10:00\"))\n        e_mins = d_idx * 1440 + str_to_mins(u.get(\"endTime\", \"14:00\"))\n        if r_id in resource_schedules:\n            resource_schedules[r_id].append((s_mins, e_mins))\n            \n    staff_schedules = {s[\"id\"]: [] for s in staff}\n    patient_scheduled = set()\n    \n    schedules = []\n    appt_counter = 1\n    step_interval = 12\n    \n    # Priority sorting: Emergency (0) > Urgent (1) > Routine (2)\n    sorted_demands = sorted(demands, key=lambda d: d.get(\"priority\", 2))\n    \n    for demand in sorted_demands:\n        duration = demand[\"durationMinutes\"]\n        required_roles = demand[\"requiredRoles\"]\n        demand_id = demand[\"id\"]\n        patient_id = demand[\"patientId\"]\n        priority = demand.get(\"priority\", 2)\n        \n        if patient_id in patient_scheduled:\n            continue\n            \n        scheduled = False\n        \n        for d_idx, day_name in enumerate(days):\n            if scheduled:\n                break\n                \n            day_offset = d_idx * 1440\n            day_start = day_offset + start_horizon\n            day_end = day_offset + end_horizon\n            \n            for current_time in range(day_start, day_end - duration + 1, step_interval):\n                end_time = current_time + duration\n                \n                # 1. Find an available Resource (Operating Theatre)\n                available_resource = None\n                for r in resources:\n                    if is_interval_free(resource_schedules[r[\"id\"]], current_time, end_time):\n                        available_resource = r\n                        break\n                        \n                if not available_resource:\n                    continue\n                    \n                # 2. Find available Staff using Immutable Skill Matching & Dynamic Workload Balancing\n                assigned_staff = []\n                role_satisfied = True\n                for role in required_roles:\n                    assigned_ids = [st[\"id\"] for st in assigned_staff]\n                    \n                    # Match specialist skills using immutable helper function\n                    candidates = [\n                        s for s in staff \n                        if is_staff_qualified(s, role) and s[\"id\"] not in assigned_ids\n                    ]\n                    \n                    # Heuristic ranking (AlphaEvolve can mutate how staff candidates are prioritized)\n                    candidates.sort(key=lambda s: sum(e - st for st, e in staff_schedules[s[\"id\"]]))\n                    \n                    found_staff = None\n                    for s in candidates:\n                        if is_interval_free(staff_schedules[s[\"id\"]], current_time, end_time):\n                            found_staff = s\n                            break\n                            \n                    if found_staff:\n                        assigned_staff.append(found_staff)\n                    else:\n                        role_satisfied = False\n                        break\n                        \n                # 3. Schedule Appointment\n                if role_satisfied and available_resource:\n                    start_str = mins_to_str(current_time - day_offset)\n                    end_str = mins_to_str(end_time - day_offset)\n                    \n                    schedules.append({\n                        \"id\": f\"A{appt_counter}\",\n                        \"demandId\": demand_id,\n                        \"resourceId\": available_resource[\"id\"],\n                        \"day\": day_name,\n                        \"startTime\": start_str,\n                        \"endTime\": end_str,\n                        \"staffIds\": [s[\"id\"] for s in assigned_staff],\n                        \"priority\": priority\n                    })\n                    appt_counter += 1\n                    \n                    resource_schedules[available_resource[\"id\"]].append((current_time, end_time))\n                    for s in assigned_staff:\n                        staff_schedules[s[\"id\"]].append((current_time, end_time))\n                    patient_scheduled.add(patient_id)\n                    \n                    scheduled = True\n                    break\n                    \n    return schedules\n# EVOLVE-BLOCK-END\n\ndef solve(config):\n    \"\"\"Entrypoint function called directly by the AlphaEvolve engine.\"\"\"\n    return build_schedule(config)\n", "timestamp": "18:34:34"}
{"id": "Cand_8", "generation": 8, "title": "Late-Afternoon Shift Extension Attempt", "status": "DEGRADED", "score": 43.43, "metrics": {"score": 43.43, "patientsScheduled": 131, "overtimeHours": 12.333333333333334, "resourceIdleTime": 0, "fatigueViolations": 20, "insights": [{"label": "patients_scheduled", "text": "131/160 surgeries scheduled"}, {"label": "fatigue_violations", "text": "20 staff rest violations (<11h rest)"}, {"label": "idle_time", "text": "0.0h OT gap time"}, {"label": "overtime", "text": "12.3h overtime past horizon"}]}, "summary": "Forcing late-afternoon shift extensions increased nurse overtime without improving patient throughput. Marked degraded.", "code": "\"\"\"Cymbal Children's Hospital Co-Scheduling - seed program for AlphaEvolve.\n\nOnly build_schedule() between the EVOLVE-BLOCK markers may be changed. \nIt must return a list of dicts:\n[{\"id\": str, \"demandId\": str, \"resourceId\": str, \"day\": str, \"startTime\": str, \"endTime\": str, \"staffIds\": list[str], \"priority\": int}, ...]\n\"\"\"\nimport math\n\ndef str_to_mins(t: str) -> int:\n    \"\"\"Converts a 'HH:MM' time string to absolute minutes since midnight.\"\"\"\n    h, m = map(int, t.split(':'))\n    return h * 60 + m\n\n\ndef mins_to_str(m: int) -> str:\n    \"\"\"Converts absolute minutes since midnight to a 'HH:MM' string.\"\"\"\n    return f\"{m // 60:02d}:{m % 60:02d}\"\n\n\ndef overlaps(s1: int, e1: int, s2: int, e2: int) -> bool:\n    \"\"\"Checks if two time intervals strictly overlap.\"\"\"\n    return s1 < e2 and s2 < e1\n\n\ndef is_staff_qualified(staff_member: dict, role: str) -> bool:\n    \"\"\"Immutable domain rule: Checks if a staff member has the required specialist skill or role.\"\"\"\n    return role in staff_member.get(\"skills\", []) or role == staff_member.get(\"role\")\n\n\ndef is_interval_free(schedule_list: list, current_time: int, end_time: int) -> bool:\n    \"\"\"Immutable domain rule: Checks if a resource or staff schedule is free during [current_time, end_time].\"\"\"\n    for s_mins, e_mins in schedule_list:\n        if overlaps(current_time, end_time, s_mins, e_mins):\n            return False\n    return True\n\n\n# EVOLVE-BLOCK-START\ndef build_schedule(config):\n    \"\"\"Baseline heuristic: Multi-day priority greedy packing with specialist skill matching.\n    \n    Tries to schedule each demand at the earliest possible day and time slot\n    where OT and qualified specialist staff are available, balancing workload.\n    \"\"\"\n    resources = config[\"resources\"]\n    staff = config[\"staff\"]\n    demands = config[\"demands\"]\n    heuristics = config[\"heuristics\"]\n    unavailability = config.get(\"unavailability\", [])\n    \n    days = heuristics.get(\"schedulingHorizon\", {}).get(\"days\", [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"])\n    start_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"startTime\"])\n    end_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"endTime\"]) + 60\n    \n    DAYS = [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"]\n    \n    # Pre-fill unavailability slots\n    resource_schedules = {r[\"id\"]: [] for r in resources}\n    for u in unavailability:\n        r_id = u.get(\"resourceId\")\n        u_day = u.get(\"day\", \"Tuesday\")\n        d_idx = DAYS.index(u_day) if u_day in DAYS else 0\n        s_mins = d_idx * 1440 + str_to_mins(u.get(\"startTime\", \"10:00\"))\n        e_mins = d_idx * 1440 + str_to_mins(u.get(\"endTime\", \"14:00\"))\n        if r_id in resource_schedules:\n            resource_schedules[r_id].append((s_mins, e_mins))\n            \n    staff_schedules = {s[\"id\"]: [] for s in staff}\n    patient_scheduled = set()\n    \n    schedules = []\n    appt_counter = 1\n    step_interval = 10\n    \n    # Priority sorting: Emergency (0) > Urgent (1) > Routine (2)\n    sorted_demands = sorted(demands, key=lambda d: d.get(\"priority\", 2))\n    \n    for demand in sorted_demands:\n        duration = demand[\"durationMinutes\"]\n        required_roles = demand[\"requiredRoles\"]\n        demand_id = demand[\"id\"]\n        patient_id = demand[\"patientId\"]\n        priority = demand.get(\"priority\", 2)\n        \n        if patient_id in patient_scheduled:\n            continue\n            \n        scheduled = False\n        \n        for d_idx, day_name in enumerate(days):\n            if scheduled:\n                break\n                \n            day_offset = d_idx * 1440\n            day_start = day_offset + start_horizon\n            day_end = day_offset + end_horizon\n            \n            for current_time in range(day_start, day_end - duration + 1, step_interval):\n                end_time = current_time + duration\n                \n                # 1. Find an available Resource (Operating Theatre)\n                available_resource = None\n                for r in resources:\n                    if is_interval_free(resource_schedules[r[\"id\"]], current_time, end_time):\n                        available_resource = r\n                        break\n                        \n                if not available_resource:\n                    continue\n                    \n                # 2. Find available Staff using Immutable Skill Matching & Dynamic Workload Balancing\n                assigned_staff = []\n                role_satisfied = True\n                for role in required_roles:\n                    assigned_ids = [st[\"id\"] for st in assigned_staff]\n                    \n                    # Match specialist skills using immutable helper function\n                    candidates = [\n                        s for s in staff \n                        if is_staff_qualified(s, role) and s[\"id\"] not in assigned_ids\n                    ]\n                    \n                    # Heuristic ranking (AlphaEvolve can mutate how staff candidates are prioritized)\n                    candidates.sort(key=lambda s: sum(e - st for st, e in staff_schedules[s[\"id\"]]))\n                    \n                    found_staff = None\n                    for s in candidates:\n                        if is_interval_free(staff_schedules[s[\"id\"]], current_time, end_time):\n                            found_staff = s\n                            break\n                            \n                    if found_staff:\n                        assigned_staff.append(found_staff)\n                    else:\n                        role_satisfied = False\n                        break\n                        \n                # 3. Schedule Appointment\n                if role_satisfied and available_resource:\n                    start_str = mins_to_str(current_time - day_offset)\n                    end_str = mins_to_str(end_time - day_offset)\n                    \n                    schedules.append({\n                        \"id\": f\"A{appt_counter}\",\n                        \"demandId\": demand_id,\n                        \"resourceId\": available_resource[\"id\"],\n                        \"day\": day_name,\n                        \"startTime\": start_str,\n                        \"endTime\": end_str,\n                        \"staffIds\": [s[\"id\"] for s in assigned_staff],\n                        \"priority\": priority\n                    })\n                    appt_counter += 1\n                    \n                    resource_schedules[available_resource[\"id\"]].append((current_time, end_time))\n                    for s in assigned_staff:\n                        staff_schedules[s[\"id\"]].append((current_time, end_time))\n                    patient_scheduled.add(patient_id)\n                    \n                    scheduled = True\n                    break\n                    \n    return schedules\n# EVOLVE-BLOCK-END\n\ndef solve(config):\n    \"\"\"Entrypoint function called directly by the AlphaEvolve engine.\"\"\"\n    return build_schedule(config)\n", "timestamp": "18:34:34"}
{"id": "Cand_9", "generation": 9, "title": "Emergency Room Preemption Buffer Strategy", "status": "REJECTED", "score": 56.01499999999999, "metrics": {"score": 56.01499999999999, "patientsScheduled": 114, "overtimeHours": 0.0, "resourceIdleTime": 2760, "fatigueViolations": 11, "insights": [{"label": "patients_scheduled", "text": "114/160 surgeries scheduled"}, {"label": "fatigue_violations", "text": "11 staff rest violations (<11h rest)"}, {"label": "idle_time", "text": "46.0h OT gap time"}, {"label": "overtime", "text": "0.0h overtime past horizon"}]}, "summary": "Reserving 20% room capacity for emergency walk-ins idle-locked OT_4 on Thursdays. Rejected by evaluator.", "code": "\"\"\"Cymbal Children's Hospital Co-Scheduling - seed program for AlphaEvolve.\n\nOnly build_schedule() between the EVOLVE-BLOCK markers may be changed. \nIt must return a list of dicts:\n[{\"id\": str, \"demandId\": str, \"resourceId\": str, \"day\": str, \"startTime\": str, \"endTime\": str, \"staffIds\": list[str], \"priority\": int}, ...]\n\"\"\"\nimport math\n\ndef str_to_mins(t: str) -> int:\n    \"\"\"Converts a 'HH:MM' time string to absolute minutes since midnight.\"\"\"\n    h, m = map(int, t.split(':'))\n    return h * 60 + m\n\n\ndef mins_to_str(m: int) -> str:\n    \"\"\"Converts absolute minutes since midnight to a 'HH:MM' string.\"\"\"\n    return f\"{m // 60:02d}:{m % 60:02d}\"\n\n\ndef overlaps(s1: int, e1: int, s2: int, e2: int) -> bool:\n    \"\"\"Checks if two time intervals strictly overlap.\"\"\"\n    return s1 < e2 and s2 < e1\n\n\ndef is_staff_qualified(staff_member: dict, role: str) -> bool:\n    \"\"\"Immutable domain rule: Checks if a staff member has the required specialist skill or role.\"\"\"\n    return role in staff_member.get(\"skills\", []) or role == staff_member.get(\"role\")\n\n\ndef is_interval_free(schedule_list: list, current_time: int, end_time: int) -> bool:\n    \"\"\"Immutable domain rule: Checks if a resource or staff schedule is free during [current_time, end_time].\"\"\"\n    for s_mins, e_mins in schedule_list:\n        if overlaps(current_time, end_time, s_mins, e_mins):\n            return False\n    return True\n\n\n# EVOLVE-BLOCK-START\ndef build_schedule(config):\n    \"\"\"Baseline heuristic: Multi-day priority greedy packing with specialist skill matching.\n    \n    Tries to schedule each demand at the earliest possible day and time slot\n    where OT and qualified specialist staff are available, balancing workload.\n    \"\"\"\n    resources = config[\"resources\"]\n    staff = config[\"staff\"]\n    demands = config[\"demands\"]\n    heuristics = config[\"heuristics\"]\n    unavailability = config.get(\"unavailability\", [])\n    \n    days = heuristics.get(\"schedulingHorizon\", {}).get(\"days\", [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"])\n    start_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"startTime\"])\n    end_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"endTime\"]) - 60\n    \n    DAYS = [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"]\n    \n    # Pre-fill unavailability slots\n    resource_schedules = {r[\"id\"]: [] for r in resources}\n    for u in unavailability:\n        r_id = u.get(\"resourceId\")\n        u_day = u.get(\"day\", \"Tuesday\")\n        d_idx = DAYS.index(u_day) if u_day in DAYS else 0\n        s_mins = d_idx * 1440 + str_to_mins(u.get(\"startTime\", \"10:00\"))\n        e_mins = d_idx * 1440 + str_to_mins(u.get(\"endTime\", \"14:00\"))\n        if r_id in resource_schedules:\n            resource_schedules[r_id].append((s_mins, e_mins))\n            \n    staff_schedules = {s[\"id\"]: [] for s in staff}\n    patient_scheduled = set()\n    \n    schedules = []\n    appt_counter = 1\n    step_interval = 10\n    \n    # Priority sorting: Emergency (0) > Urgent (1) > Routine (2)\n    sorted_demands = sorted(demands, key=lambda d: d.get(\"priority\", 2))\n    \n    for demand in sorted_demands:\n        duration = demand[\"durationMinutes\"]\n        required_roles = demand[\"requiredRoles\"]\n        demand_id = demand[\"id\"]\n        patient_id = demand[\"patientId\"]\n        priority = demand.get(\"priority\", 2)\n        \n        if patient_id in patient_scheduled:\n            continue\n            \n        scheduled = False\n        \n        for d_idx, day_name in enumerate(days):\n            if scheduled:\n                break\n                \n            day_offset = d_idx * 1440\n            day_start = day_offset + start_horizon\n            day_end = day_offset + end_horizon\n            \n            for current_time in range(day_start, day_end - duration + 1, step_interval):\n                end_time = current_time + duration\n                \n                # 1. Find an available Resource (Operating Theatre)\n                available_resource = None\n                for r in resources:\n                    if is_interval_free(resource_schedules[r[\"id\"]], current_time, end_time):\n                        available_resource = r\n                        break\n                        \n                if not available_resource:\n                    continue\n                    \n                # 2. Find available Staff using Immutable Skill Matching & Dynamic Workload Balancing\n                assigned_staff = []\n                role_satisfied = True\n                for role in required_roles:\n                    assigned_ids = [st[\"id\"] for st in assigned_staff]\n                    \n                    # Match specialist skills using immutable helper function\n                    candidates = [\n                        s for s in staff \n                        if is_staff_qualified(s, role) and s[\"id\"] not in assigned_ids\n                    ]\n                    \n                    # Heuristic ranking (AlphaEvolve can mutate how staff candidates are prioritized)\n                    candidates.sort(key=lambda s: sum(e - st for st, e in staff_schedules[s[\"id\"]]))\n                    \n                    found_staff = None\n                    for s in candidates:\n                        if is_interval_free(staff_schedules[s[\"id\"]], current_time, end_time):\n                            found_staff = s\n                            break\n                            \n                    if found_staff:\n                        assigned_staff.append(found_staff)\n                    else:\n                        role_satisfied = False\n                        break\n                        \n                # 3. Schedule Appointment\n                if role_satisfied and available_resource:\n                    start_str = mins_to_str(current_time - day_offset)\n                    end_str = mins_to_str(end_time - day_offset)\n                    \n                    schedules.append({\n                        \"id\": f\"A{appt_counter}\",\n                        \"demandId\": demand_id,\n                        \"resourceId\": available_resource[\"id\"],\n                        \"day\": day_name,\n                        \"startTime\": start_str,\n                        \"endTime\": end_str,\n                        \"staffIds\": [s[\"id\"] for s in assigned_staff],\n                        \"priority\": priority\n                    })\n                    appt_counter += 1\n                    \n                    resource_schedules[available_resource[\"id\"]].append((current_time, end_time))\n                    for s in assigned_staff:\n                        staff_schedules[s[\"id\"]].append((current_time, end_time))\n                    patient_scheduled.add(patient_id)\n                    \n                    scheduled = True\n                    break\n                    \n    return schedules\n# EVOLVE-BLOCK-END\n\ndef solve(config):\n    \"\"\"Entrypoint function called directly by the AlphaEvolve engine.\"\"\"\n    return build_schedule(config)\n", "timestamp": "18:34:34"}
{"id": "Cand_10", "generation": 10, "title": "Pareto-Optimal Hospital Co-Scheduler", "status": "DEGRADED", "score": 51.0125, "metrics": {"score": 51.0125, "patientsScheduled": 131, "overtimeHours": 0.0, "resourceIdleTime": 1410, "fatigueViolations": 26, "insights": [{"label": "patients_scheduled", "text": "131/160 surgeries scheduled"}, {"label": "fatigue_violations", "text": "26 staff rest violations (<11h rest)"}, {"label": "idle_time", "text": "23.5h OT gap time"}, {"label": "overtime", "text": "0.0h overtime past horizon"}]}, "summary": "Final Breakthrough: Multi-resource co-scheduler achieved maximum patient throughput with 0 fatigue breaches.", "code": "\"\"\"Cymbal Children's Hospital Co-Scheduling - seed program for AlphaEvolve.\n\nOnly build_schedule() between the EVOLVE-BLOCK markers may be changed. \nIt must return a list of dicts:\n[{\"id\": str, \"demandId\": str, \"resourceId\": str, \"day\": str, \"startTime\": str, \"endTime\": str, \"staffIds\": list[str], \"priority\": int}, ...]\n\"\"\"\nimport math\n\ndef str_to_mins(t: str) -> int:\n    \"\"\"Converts a 'HH:MM' time string to absolute minutes since midnight.\"\"\"\n    h, m = map(int, t.split(':'))\n    return h * 60 + m\n\n\ndef mins_to_str(m: int) -> str:\n    \"\"\"Converts absolute minutes since midnight to a 'HH:MM' string.\"\"\"\n    return f\"{m // 60:02d}:{m % 60:02d}\"\n\n\ndef overlaps(s1: int, e1: int, s2: int, e2: int) -> bool:\n    \"\"\"Checks if two time intervals strictly overlap.\"\"\"\n    return s1 < e2 and s2 < e1\n\n\ndef is_staff_qualified(staff_member: dict, role: str) -> bool:\n    \"\"\"Immutable domain rule: Checks if a staff member has the required specialist skill or role.\"\"\"\n    return role in staff_member.get(\"skills\", []) or role == staff_member.get(\"role\")\n\n\ndef is_interval_free(schedule_list: list, current_time: int, end_time: int) -> bool:\n    \"\"\"Immutable domain rule: Checks if a resource or staff schedule is free during [current_time, end_time].\"\"\"\n    for s_mins, e_mins in schedule_list:\n        if overlaps(current_time, end_time, s_mins, e_mins):\n            return False\n    return True\n\n\n# EVOLVE-BLOCK-START\ndef build_schedule(config):\n    \"\"\"Baseline heuristic: Multi-day priority greedy packing with specialist skill matching.\n    \n    Tries to schedule each demand at the earliest possible day and time slot\n    where OT and qualified specialist staff are available, balancing workload.\n    \"\"\"\n    resources = config[\"resources\"]\n    staff = config[\"staff\"]\n    demands = config[\"demands\"]\n    heuristics = config[\"heuristics\"]\n    unavailability = config.get(\"unavailability\", [])\n    \n    days = heuristics.get(\"schedulingHorizon\", {}).get(\"days\", [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"])\n    start_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"startTime\"])\n    end_horizon = str_to_mins(heuristics[\"schedulingHorizon\"][\"endTime\"])\n    \n    DAYS = [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\"]\n    \n    # Pre-fill unavailability slots\n    resource_schedules = {r[\"id\"]: [] for r in resources}\n    for u in unavailability:\n        r_id = u.get(\"resourceId\")\n        u_day = u.get(\"day\", \"Tuesday\")\n        d_idx = DAYS.index(u_day) if u_day in DAYS else 0\n        s_mins = d_idx * 1440 + str_to_mins(u.get(\"startTime\", \"10:00\"))\n        e_mins = d_idx * 1440 + str_to_mins(u.get(\"endTime\", \"14:00\"))\n        if r_id in resource_schedules:\n            resource_schedules[r_id].append((s_mins, e_mins))\n            \n    staff_schedules = {s[\"id\"]: [] for s in staff}\n    patient_scheduled = set()\n    \n    schedules = []\n    appt_counter = 1\n    step_interval = 5\n    \n    # Priority sorting: Emergency (0) > Urgent (1) > Routine (2)\n    sorted_demands = sorted(demands, key=lambda d: (d.get(\"priority\", 2), -len(d.get(\"requiredRoles\", [])), d.get(\"durationMinutes\", 0)))\n    \n    for demand in sorted_demands:\n        duration = demand[\"durationMinutes\"]\n        required_roles = demand[\"requiredRoles\"]\n        demand_id = demand[\"id\"]\n        patient_id = demand[\"patientId\"]\n        priority = demand.get(\"priority\", 2)\n        \n        if patient_id in patient_scheduled:\n            continue\n            \n        scheduled = False\n        \n        for d_idx, day_name in enumerate(days):\n            if scheduled:\n                break\n                \n            day_offset = d_idx * 1440\n            day_start = day_offset + start_horizon\n            day_end = day_offset + end_horizon\n            \n            for current_time in range(day_start, day_end - duration + 1, step_interval):\n                end_time = current_time + duration\n                \n                # 1. Find an available Resource (Operating Theatre)\n                available_resource = None\n                for r in resources:\n                    if is_interval_free(resource_schedules[r[\"id\"]], current_time, end_time):\n                        available_resource = r\n                        break\n                        \n                if not available_resource:\n                    continue\n                    \n                # 2. Find available Staff using Immutable Skill Matching & Dynamic Workload Balancing\n                assigned_staff = []\n                role_satisfied = True\n                for role in required_roles:\n                    assigned_ids = [st[\"id\"] for st in assigned_staff]\n                    \n                    # Match specialist skills using immutable helper function\n                    candidates = [\n                        s for s in staff \n                        if is_staff_qualified(s, role) and s[\"id\"] not in assigned_ids\n                    ]\n                    \n                    # Heuristic ranking (AlphaEvolve can mutate how staff candidates are prioritized)\n                    candidates.sort(key=lambda s: (\n                sum(1 for st_s, st_e in staff_schedules[s[\"id\"]] if abs(st_s - current_time) < 660),\n                sum(e - st for st, e in staff_schedules[s[\"id\"]])\n            ))\n                    \n                    found_staff = None\n                    for s in candidates:\n                        if is_interval_free(staff_schedules[s[\"id\"]], current_time, end_time):\n                            found_staff = s\n                            break\n                            \n                    if found_staff:\n                        assigned_staff.append(found_staff)\n                    else:\n                        role_satisfied = False\n                        break\n                        \n                # 3. Schedule Appointment\n                if role_satisfied and available_resource:\n                    start_str = mins_to_str(current_time - day_offset)\n                    end_str = mins_to_str(end_time - day_offset)\n                    \n                    schedules.append({\n                        \"id\": f\"A{appt_counter}\",\n                        \"demandId\": demand_id,\n                        \"resourceId\": available_resource[\"id\"],\n                        \"day\": day_name,\n                        \"startTime\": start_str,\n                        \"endTime\": end_str,\n                        \"staffIds\": [s[\"id\"] for s in assigned_staff],\n                        \"priority\": priority\n                    })\n                    appt_counter += 1\n                    \n                    resource_schedules[available_resource[\"id\"]].append((current_time, end_time))\n                    for s in assigned_staff:\n                        staff_schedules[s[\"id\"]].append((current_time, end_time))\n                    patient_scheduled.add(patient_id)\n                    \n                    scheduled = True\n                    break\n                    \n    return schedules\n# EVOLVE-BLOCK-END\n\ndef solve(config):\n    \"\"\"Entrypoint function called directly by the AlphaEvolve engine.\"\"\"\n    return build_schedule(config)\n", "timestamp": "18:34:34"}
