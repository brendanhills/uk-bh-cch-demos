---
description: Implement and verify a fix for a specific bug, marking it as Fix Implemented.
---

# Conductor bug-fix

Read `commands/bugFix.toml` and follow the `prompt` instructions to analyze, implement, and verify a fix for a bug. You can specify a bug ID directly (e.g. `/conductor:bug-fix 14`).
