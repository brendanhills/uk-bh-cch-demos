---
description: Display a summary table of unresolved bugs in the workspace.
---

# Conductor bug-list

Read `commands/bugList.toml` and follow the `prompt` instructions to list active bugs. Use the argument `all` to see resolved bugs.
